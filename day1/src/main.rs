//use core::fmt;
//use std::{fs, string, vec};

fn main() {
    assert_eq!(62, 62);
    assert_eq!(95356, 95356);

    assert_eq!(1, 3);
    assert_eq!(2, 4);
}


fn parse(input: &str, _part1: bool) -> usize {
    1
}
